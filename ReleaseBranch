pipeline {
    agent any
    options { skipDefaultCheckout() }
      stages {
		
		stage('Build and Package') {
		when {
    branch 'Rel*'
    }
				steps {
        
        echo "pipeline"
        
        }
     }
  }
 }
				
				  
