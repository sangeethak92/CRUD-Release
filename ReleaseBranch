pipeline {
    agent any
    options { skipDefaultCheckout() }
      stages {
		
		stage('Build and Package') {
		when {
		   anyOf { 
		      branch 'Re*' 
    
                         }
			 }
				steps {
        
        echo "pipeline"
        
        }
     }
  }
 }
				
				  
